---
title: Text Snippets
description: Reusable text values from inline, file, command, or directory sources
icon: clipboard
---

The native interface supports text snippets, which let you define reusable text values that can be searched and copied to the clipboard or typed into applications.

<Note>
Text snippets are only available in **native mode**. They are not available when using Fuzzel mode.
</Note>

## Overview

Snippets can come from four different sources:

<CardGroup cols={2}>
  <Card title="Inline" icon="code">
    Defined directly in the config file
  </Card>
  <Card title="File" icon="file">
    Loaded from an external YAML file
  </Card>
  <Card title="Command" icon="terminal">
    Generated by running a command (Alfred JSON format)
  </Card>
  <Card title="Directory" icon="folder">
    Loaded from `.snippet` files in a directory
  </Card>
</CardGroup>

## Usage

<Steps>
  <Step title="Type Keyword">
    Type the configured keyword (e.g., `em` for emails)
  </Step>
  <Step title="Filter Results">
    Continue typing to fuzzy-filter snippets by name
  </Step>
  <Step title="Select Snippet">
    Use arrow keys to select the desired snippet
  </Step>
  <Step title="Execute Action">
    - **Enter**: Default action (copy or insert)
    - **Ctrl+Enter**: Secondary action (if configured)
  </Step>
</Steps>

## Configuration Sources

### Inline Snippets

Define snippets directly in `raffi.yaml`:

```yaml raffi.yaml
addons:
  text_snippets:
    - name: "Emails"
      keyword: "em"
      icon: "mail"
      snippets:
        - name: "Personal Email"
          value: "user@example.com"
        - name: "Work Email"
          value: "user@company.com"
        - name: "Support Email"
          value: "support@company.com"
```

Usage:
```text
em personal
# Filters to "Personal Email"
# Press Enter to copy user@example.com
```

### File Source

Load snippets from an external YAML file:

```yaml raffi.yaml
addons:
  text_snippets:
    - name: "Templates"
      keyword: "tpl"
      icon: "document"
      file: "~/.config/raffi/snippets.yaml"
```

The snippet file (`~/.config/raffi/snippets.yaml`):

```yaml snippets.yaml
- name: "Greeting"
  value: "Hello, world!"

- name: "Signature"
  value: "Best regards, User"

- name: "Meeting Template"
  value: |
    Meeting Notes
    Date: [DATE]
    Attendees: [NAMES]
    Topics:
    - 
    Action Items:
    - 
```

<Note>
File-based snippets are cached per session for performance.
</Note>

### Command Source

Generate snippets dynamically from a command:

```yaml raffi.yaml
addons:
  text_snippets:
    - name: "Dynamic"
      keyword: "dyn"
      icon: "terminal"
      command: "my-snippet-gen"
      args: ["-j"]
```

The command must output JSON in Alfred Script Filter format:

```json
{
  "items": [
    {
      "title": "Current Date",
      "arg": "2024-03-15"
    },
    {
      "title": "Current Time",
      "arg": "14:30:00"
    }
  ]
}
```

<Tip>
The `title` field becomes the snippet name, and `arg` becomes the snippet value.
</Tip>

### Directory Source

Load snippets from a directory of `.snippet` files:

```yaml raffi.yaml
addons:
  text_snippets:
    - name: "Snippets"
      keyword: "sn"
      icon: "snippets"
      directory: "~/.local/share/desktop-config/snippets"
```

Each `.snippet` file has this format:

```text greeting.snippet
Friendly Greeting
---
Hello! How are you doing today?
```

```text email-signature.snippet
Professional Signature
---
Best regards,
John Doe
Software Engineer
john@example.com
```

Format:
- **First line**: Snippet name (description)
- **`---`**: Separator
- **After separator**: Snippet content (value)

<Note>
Directory-based snippets are sorted alphabetically and cached per session.
</Note>

## Actions

Configure how snippets are used when selected:

### Default Actions

<Tabs>
  <Tab title="Copy (Default)">
    ```yaml
    action: "copy"
    ```
    Copies snippet to clipboard using `wl-copy`, `xclip`, or `xsel`
  </Tab>
  
  <Tab title="Insert/Type">
    ```yaml
    action: "insert"
    ```
    Types snippet into focused app using `wtype` or `ydotool`
  </Tab>
</Tabs>

### Custom Actions

Use command templates with `{value}` placeholder:

```yaml raffi.yaml
text_snippets:
  - name: "Commands"
    keyword: "cmd"
    action: "kitty sh -c {value}"
    snippets:
      - name: "System Update"
        value: "sudo pacman -Syu"
```

### Primary and Secondary Actions

Configure different actions for Enter and Ctrl+Enter:

```yaml raffi.yaml
text_snippets:
  - name: "Emails"
    keyword: "em"
    icon: "mail"
    action: "insert"          # Enter: Type into app
    secondary_action: "copy"  # Ctrl+Enter: Copy to clipboard
    snippets:
      - name: "Personal Email"
        value: "user@example.com"
      - name: "Work Email"
        value: "user@company.com"
```

## Configuration Fields

<ParamField path="name" type="string" required>
  Display name shown during loading (for command sources)
</ParamField>

<ParamField path="keyword" type="string" required>
  Text that activates the snippet source
</ParamField>

<ParamField path="icon" type="string">
  Icon name from your icon cache to display next to each snippet
</ParamField>

<ParamField path="snippets" type="array">
  Inline list of snippets (each with `name` and `value`)
</ParamField>

<ParamField path="file" type="string">
  Path to a YAML file containing snippets (cached per session)
</ParamField>

<ParamField path="command" type="string">
  Executable that outputs Alfred Script Filter JSON
</ParamField>

<ParamField path="directory" type="string">
  Path to a directory of `.snippet` files (cached per session)
</ParamField>

<ParamField path="args" type="array">
  Arguments passed to the command
</ParamField>

<ParamField path="action" type="string" default="copy">
  Action on Enter:
  - `"copy"`: Copy to clipboard
  - `"insert"`: Type into focused app
  - Command template with `{value}` placeholder
</ParamField>

<ParamField path="secondary_action" type="string" default="insert">
  Action on Ctrl+Enter. Accepts same values as `action`.
</ParamField>

<Warning>
Exactly one of `snippets`, `file`, `command`, or `directory` should be specified per entry.
</Warning>

## Examples

### Email Addresses

```yaml raffi.yaml
text_snippets:
  - name: "Emails"
    keyword: "em"
    icon: "mail"
    snippets:
      - name: "Personal"
        value: "john@personal.com"
      - name: "Work"
        value: "john@company.com"
      - name: "GitHub"
        value: "john@users.noreply.github.com"
```

### Code Snippets

```yaml raffi.yaml
text_snippets:
  - name: "Code Templates"
    keyword: "code"
    icon: "code"
    action: "insert"
    snippets:
      - name: "Rust Main"
        value: |
          fn main() {
              println!("Hello, world!");
          }
      
      - name: "Python Main"
        value: |
          if __name__ == "__main__":
              pass
```

### Git Commands

```yaml raffi.yaml
text_snippets:
  - name: "Git Commands"
    keyword: "git"
    file: "~/.config/raffi/git-snippets.yaml"
```

```yaml git-snippets.yaml
- name: "Amend Commit"
  value: "git commit --amend --no-edit"

- name: "Push Force"
  value: "git push --force-with-lease"

- name: "Interactive Rebase"
  value: "git rebase -i HEAD~"
```

### Dynamic Dates

Create a script that generates date snippets:

```python date-snippets.py
#!/usr/bin/env python3
import json
from datetime import datetime, timedelta

today = datetime.now()

items = [
    {"title": "Today", "arg": today.strftime("%Y-%m-%d")},
    {"title": "Yesterday", "arg": (today - timedelta(days=1)).strftime("%Y-%m-%d")},
    {"title": "Tomorrow", "arg": (today + timedelta(days=1)).strftime("%Y-%m-%d")},
    {"title": "ISO Timestamp", "arg": today.isoformat()},
]

print(json.dumps({"items": items}))
```

```yaml raffi.yaml
text_snippets:
  - name: "Dates"
    keyword: "date"
    command: "python3 ~/.config/raffi/date-snippets.py"
    icon: "calendar"
```

## Use Cases

<AccordionGroup>
  <Accordion title="Contact Information">
    ```yaml
    - name: "Contact"
      keyword: "contact"
      snippets:
        - name: "Email"
          value: "user@example.com"
        - name: "Phone"
          value: "+1-555-0123"
        - name: "Address"
          value: "123 Main St, City, State 12345"
    ```
  </Accordion>

  <Accordion title="Frequently Used Commands">
    ```yaml
    - name: "Shell Commands"
      keyword: "sh"
      action: "insert"
      snippets:
        - name: "Docker Cleanup"
          value: "docker system prune -af"
        - name: "Find Large Files"
          value: "du -sh * | sort -h"
    ```
  </Accordion>

  <Accordion title="Text Templates">
    ```yaml
    - name: "Templates"
      keyword: "tpl"
      directory: "~/.config/raffi/templates"
    ```
    
    With files like:
    - `meeting-notes.snippet`
    - `bug-report.snippet`
    - `email-signature.snippet`
  </Accordion>

  <Accordion title="Programming Snippets">
    ```yaml
    - name: "Code"
      keyword: "snippet"
      file: "~/.config/raffi/code-snippets.yaml"
      action: "insert"
    ```
  </Accordion>
</AccordionGroup>

## Keyboard Shortcuts

- **Enter**: Execute primary action (default: copy)
- **Ctrl+Enter**: Execute secondary action (default: insert)
- **Esc**: Cancel and return to normal mode
- **↑/↓**: Navigate through snippets

## Requirements

<Note>
**For copying**: `wl-copy` (wl-clipboard), `xclip`, or `xsel`
</Note>

<Note>
**For typing/inserting**: `wtype` or `ydotool`
</Note>

## Tips

<Tip>
Use file sources for large snippet collections to keep your main config clean
</Tip>

<Tip>
Command sources are perfect for dynamic content like dates, git branch names, or API responses
</Tip>

<Tip>
Directory sources work well for organizing snippets by category (each category in its own directory)
</Tip>

<Tip>
Set `action: "insert"` and `secondary_action: "copy"` for maximum flexibility
</Tip>

## Complete Example

Here's a full configuration with multiple snippet sources:

```yaml raffi.yaml
addons:
  text_snippets:
    # Inline emails
    - name: "Emails"
      keyword: "em"
      icon: "mail"
      action: "copy"
      snippets:
        - name: "Personal"
          value: "user@example.com"
        - name: "Work"
          value: "user@company.com"
    
    # Templates from file
    - name: "Templates"
      keyword: "tpl"
      icon: "document"
      file: "~/.config/raffi/templates.yaml"
      action: "insert"
    
    # Dynamic dates from command
    - name: "Dates"
      keyword: "date"
      command: "date-snippets"
      icon: "calendar"
    
    # Code snippets from directory
    - name: "Code Snippets"
      keyword: "code"
      directory: "~/.config/raffi/snippets"
      action: "insert"
      secondary_action: "copy"
```
