---
title: General Settings
description: Configure Raffi's UI, theme, fonts, and window properties
---

## Overview

The `general` section in your configuration file sets persistent defaults for Raffi's appearance and behavior. These settings are equivalent to command-line flags but are applied automatically on every launch.

<Note>
Command-line flags always override settings from the configuration file.
</Note>

## Configuration Options

All fields in the `general` section are optional.

<ParamField path="ui_type" type="string" default="fuzzel">
  UI backend selection. Valid options:
  - `fuzzel` - Uses external Fuzzel launcher (default)
  - `native` - Uses built-in iced-based GUI
  
  Equivalent to `-u` or `--ui-type` flag.
</ParamField>

<ParamField path="theme" type="string" default="dark">
  Color theme for the native interface. Valid options:
  - `dark` - Dracula-inspired palette (default)
  - `light` - Rose Pine Dawn palette
  
  Equivalent to `-t` or `--theme` flag. Only applies to native UI.
</ParamField>

<ParamField path="no_icons" type="boolean" default="false">
  Disables icon loading for faster startup.
  
  Equivalent to `-I` or `--disable-icons` flag.
</ParamField>

<ParamField path="default_script_shell" type="string" default="bash">
  Default interpreter for inline scripts. Common values:
  - `bash` (default)
  - `/bin/zsh`
  - `python3`
  
  Equivalent to `--default-script-shell` flag.
</ParamField>

<ParamField path="max_history" type="number" default="10">
  Maximum number of search query history entries.
  
  Set to `0` to disable history. Navigate history with Alt+P (previous) and Alt+N (next) in native mode.
</ParamField>

## Font and Typography

<ParamField path="font_size" type="number" default="20">
  Base font size in pixels. Other UI elements scale proportionally:
  - Input text: 1.2× base size (24px by default)
  - Item descriptions: 1.0× base size (20px by default)
  - Subtitles: 0.7× base size (14px by default)
  - Hints: 0.6× base size (12px by default)
</ParamField>

<ParamField path="font_family" type="string">
  Font family name (e.g., `"Inter"`, `"Fira Sans"`, `"Roboto"`).
  
  When omitted, the system default sans-serif font is used.
</ParamField>

## Window Dimensions

<ParamField path="window_width" type="number" default="800">
  Launcher window width in pixels (native mode only).
</ParamField>

<ParamField path="window_height" type="number" default="600">
  Launcher window height in pixels (native mode only).
</ParamField>

<ParamField path="padding" type="number" default="20">
  Outer window padding in pixels.
  
  When not set, padding scales automatically with `font_size`.
</ParamField>

## Theme Colors

Customize individual theme colors under the `theme_colors` section. Each field accepts hex color strings in the format `#RGB`, `#RRGGBB`, or `#RRGGBBAA`.

Only specified colors are overridden; the rest come from the base theme selected via `theme`.

<ParamField path="theme_colors.bg_base" type="string">
  Base background color.
</ParamField>

<ParamField path="theme_colors.bg_input" type="string">
  Input field background color.
</ParamField>

<ParamField path="theme_colors.accent" type="string">
  Primary accent color (used for highlights and selected items).
</ParamField>

<ParamField path="theme_colors.accent_hover" type="string">
  Accent color for hover states.
</ParamField>

<ParamField path="theme_colors.text_main" type="string">
  Primary text color.
</ParamField>

<ParamField path="theme_colors.text_muted" type="string">
  Muted/secondary text color.
</ParamField>

<ParamField path="theme_colors.selection_bg" type="string">
  Background color for selected items.
</ParamField>

<ParamField path="theme_colors.border" type="string">
  Border color.
</ParamField>

## Example Configurations

### Native UI with Light Theme

```yaml
general:
  ui_type: native
  theme: light
  font_size: 18
  font_family: "Inter"
  window_width: 900
  window_height: 500
```

### Custom Theme Colors (Catppuccin Mocha)

```yaml
general:
  theme: dark
  theme_colors:
    bg_base: "#1e1e2e"
    bg_input: "#313244"
    accent: "#cba6f7"
    accent_hover: "#89b4fa"
    text_main: "#cdd6f4"
    text_muted: "#6c7086"
    selection_bg: "#45475a"
    border: "#585b70"
```

### Minimal Fuzzel Configuration

```yaml
general:
  ui_type: fuzzel
  no_icons: true
  default_script_shell: /bin/zsh
```

## Command-Line Override

All general settings can be overridden with command-line flags:

```bash
# Override UI type
raffi --ui-type native

# Override theme
raffi --theme light

# Disable icons
raffi --disable-icons

# Set custom script shell
raffi --default-script-shell /bin/zsh
```
