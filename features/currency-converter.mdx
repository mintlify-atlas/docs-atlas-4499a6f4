---
title: Currency Converter
description: Real-time currency conversion with cached exchange rates
icon: dollar-sign
---

The native interface includes a currency converter with real-time exchange rates from the Frankfurter API.

<Note>
The currency converter is only available in **native mode**. It is not available when using Fuzzel mode.
</Note>

## Overview

Convert between currencies by typing an amount with a trigger character (default `$`) followed by target currency codes. Exchange rates are cached for one hour to minimize API requests.

<Frame>
  <img src="https://github.com/user-attachments/assets/aaf35e3f-1cef-4604-b87a-ecfa626300c1" alt="Currency converter example" />
</Frame>

## Usage

### Basic Syntax

```text
<trigger><amount> to <currency>
<trigger><amount><source> to <target>
```

### Examples

<CodeGroup>
```text Default Currency
$10 to eur
# Converts 10 USD to EUR
```

```text Explicit Source
$50 gbp to usd
# Converts 50 GBP to USD
```

```text No Spaces
$100eur to jpy
# Converts 100 EUR to JPY
```

```text Custom Trigger
€10 to usd
# With trigger set to €
```
</CodeGroup>

## Configuration

Configure the currency converter in your `raffi.yaml`:

```yaml raffi.yaml
addons:
  currency:
    enabled: true
    trigger: "$"
    default_currency: USD
    currencies:
      - USD
      - EUR
      - GBP
      - JPY
      - CAD
      - AUD
```

### Configuration Options

<ParamField path="enabled" type="boolean" default="true">
  Enable or disable the currency converter addon
</ParamField>

<ParamField path="trigger" type="string" default="$">
  Character that activates currency conversion. Common options: `$`, `€`, `£`, `¥`
</ParamField>

<ParamField path="default_currency" type="string" default="USD">
  Source currency when none is specified (e.g., `$10 to EUR` assumes USD)
</ParamField>

<ParamField path="currencies" type="array">
  List of currency codes available for conversion. If omitted, all currencies from Frankfurter API are available.
</ParamField>

### Custom Trigger

<Tabs>
  <Tab title="Dollar ($)">
    ```yaml
    addons:
      currency:
        trigger: "$"
        default_currency: USD
    ```
    
    Usage: `$100 to eur`
  </Tab>
  
  <Tab title="Euro (€)">
    ```yaml
    addons:
      currency:
        trigger: "€"
        default_currency: EUR
    ```
    
    Usage: `€100 to usd`
  </Tab>
  
  <Tab title="Pound (£)">
    ```yaml
    addons:
      currency:
        trigger: "£"
        default_currency: GBP
    ```
    
    Usage: `£100 to eur`
  </Tab>
</Tabs>

### Supported Currencies

You can specify which currencies are available:

```yaml raffi.yaml
addons:
  currency:
    enabled: true
    currencies:
      - USD  # US Dollar
      - EUR  # Euro
      - GBP  # British Pound
      - JPY  # Japanese Yen
      - CAD  # Canadian Dollar
      - AUD  # Australian Dollar
      - CHF  # Swiss Franc
      - CNY  # Chinese Yuan
      - INR  # Indian Rupee
      - BRL  # Brazilian Real
```

<Tip>
Leave the `currencies` field empty to allow conversion between all currencies supported by the Frankfurter API.
</Tip>

## Exchange Rate API

Raffi uses the [Frankfurter API](https://www.frankfurter.app/) for exchange rates:

- Free and open-source
- No API key required
- Updated daily with European Central Bank rates
- Rates are cached for 1 hour locally

### Cache Behavior

<Steps>
  <Step title="First Request">
    Fetches live rates from Frankfurter API
  </Step>
  <Step title="Subsequent Requests">
    Uses cached rates (valid for 1 hour)
  </Step>
  <Step title="After 1 Hour">
    Automatically fetches fresh rates on next conversion
  </Step>
</Steps>

## Conversion Patterns

<AccordionGroup>
  <Accordion title="Implicit Source Currency">
    When you don't specify a source currency, the `default_currency` is used:
    
    ```text
    $10 to eur      # 10 USD → EUR
    $50 to gbp      # 50 USD → GBP
    $100 to jpy     # 100 USD → JPY
    ```
  </Accordion>

  <Accordion title="Explicit Source Currency">
    Specify both source and target currencies:
    
    ```text
    $10 usd to eur  # 10 USD → EUR
    $50 gbp to usd  # 50 GBP → USD
    $100 eur to jpy # 100 EUR → JPY
    ```
  </Accordion>

  <Accordion title="Compact Format">
    No spaces between amount and currency:
    
    ```text
    $100eur to jpy  # 100 EUR → JPY
    $50gbp to usd   # 50 GBP → USD
    ```
  </Accordion>
</AccordionGroup>

## Keyboard Shortcuts

- **Enter**: Copy conversion result to clipboard
- **Esc**: Clear input and return to normal mode

## Disable Currency Converter

To disable the currency converter:

```yaml raffi.yaml
addons:
  currency:
    enabled: false
```

## Requirements

<Warning>
  Internet connection required for fetching exchange rates (cached for 1 hour)
</Warning>

<Note>
  The converter requires `wl-copy` to copy results to clipboard
</Note>

## Examples

<CardGroup cols={2}>
  <Card title="Tourist">
    ```text
    $1000 to eur
    # Planning trip to Europe
    ```
  </Card>
  
  <Card title="Shopping">
    ```text
    $99 usd to gbp
    # Compare prices
    ```
  </Card>
  
  <Card title="Freelancer">
    ```text
    €500 to usd
    # Invoice conversion
    ```
  </Card>
  
  <Card title="Investor">
    ```text
    $10000 to jpy
    # Currency exchange
    ```
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="No conversion results">
    1. Check internet connection
    2. Verify currency codes are correct (3-letter ISO codes)
    3. Ensure `enabled: true` in config
    4. Check that Frankfurter API is accessible
  </Accordion>

  <Accordion title="Wrong default currency">
    Set `default_currency` in your config:
    
    ```yaml
    addons:
      currency:
        default_currency: EUR  # Change from USD to EUR
    ```
  </Accordion>

  <Accordion title="Trigger not working">
    Make sure your trigger character doesn't conflict with other features:
    
    ```yaml
    addons:
      currency:
        trigger: "$"  # Or try "€", "£", etc.
    ```
  </Accordion>
</AccordionGroup>
